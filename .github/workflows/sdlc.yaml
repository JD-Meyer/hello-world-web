name: Hello World

on:
  push:
    branches:
      - main
    paths:
      - .

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
        ref: ${{ github.ref }}

        name: Run a one line script
        run: echo "Hello World!"
